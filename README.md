# Оптимизатор маршрутов доставки топлива

Веб-приложение для оптимизации маршрутов доставки топлива с использованием React, TypeScript и Vite.

## Возможности

- 📊 Дашборд с аналитикой
- 🚛 Управление автопарком
- ⛽ Управление АЗС
- 🗺️ Планировщик маршрутов с картой
- 📝 Журнал доставок
- 📈 Отчеты

## Требования

- Node.js (версия 18 или выше)
- npm

## Установка и запуск

### Способ 1: Через командную строку

1. Установите зависимости:
   ```bash
   npm install
   ```

2. (опционально) создайте файл `.env.local` в корне проекта и задайте ключ Gemini:
   ```
   VITE_GEMINI_API_KEY=ваш_API_ключ
   ```

3. Запустите dev-сервер:
   ```bash
   npm run dev
   ```

4. Откройте браузер по адресу, который покажет Vite (обычно `http://localhost:5173/`)

### Способ 2: Через BAT-файл (Windows)

Просто дважды кликните на файл `start-dev.bat` - он автоматически запустит dev-сервер.

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist`. Для предпросмотра сборки:

```bash
npm run preview
```

## Данные для входа

- **Логин:** `logist`
- **Пароль:** `password123`

## Технологии

- React 19
- TypeScript
- Vite
- Tailwind CSS
- Leaflet (карты)
- Recharts (графики)
